#ifndef PRIMETEST_IS_PRIME_H
#define PRIMETEST_IS_PRIME_H
/* The public interface for the static library */
#include <stdbool.h>

bool is_prime(unsigned long long n, unsigned k);

#endif // PRIMETES_IS_PRIME_H

